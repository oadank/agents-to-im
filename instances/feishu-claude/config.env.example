# agents-to-im 实例配置文件

> **注意：** 请勿在版本库中存储真实的密钥！
> 复制此文件到 `~/.agents-to-im-claude/config.env` 并填写真实密钥。

## 必需配置

```bash
CTI_DEFAULT_WORKDIR=/opt
CTI_FEISHU_APP_ID=cli_XXXXXXXXXXXXX
CTI_FEISHU_APP_SECRET=XXXXXXXXXXXXXXXXXXXXXXXXX
CTI_FEISHU_SHOW_TOOL_CALL_CARDS=false
CTI_DEFAULT_RUNTIME=claude
CTI_DASHBOARD_PORT=13579
CTI_DISABLE_PERMISSION_CHECK=true

# 分割线配置（可选）
CTI_FEISHU_SHOW_AGENT_DIVIDER=true
CTI_AGENT_NAME=feishu-claude
```

## 分割线配置说明

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `CTI_FEISHU_SHOW_AGENT_DIVIDER` | 是否在消息底部显示分割线 | `true` |
| `CTI_AGENT_NAME` | Agent 名称（用于分割线显示） | 实例 profile ID |

## 变量说明

| 变量 | 说明 | 示例值 |
|------|------|--------|
| `CTI_DEFAULT_WORKDIR` | 默认工作目录 | `/opt` 或 `~/workspace` |
| `CTI_FEISHU_APP_ID` | 飞书应用 ID | `cli_XXXXXXXX` |
| `CTI_FEISHU_APP_SECRET` | 飞书应用密钥 | 从飞书开放平台获取 |
| `CTI_FEISHU_SHOW_TOOL_CALL_CARDS` | 是否显示工具调用卡片 | `true` 或 `false` |
| `CTI_DEFAULT_RUNTIME` | 默认运行时 | `claude` 或 `codex` |
| `CTI_DASHBOARD_PORT` | 控制面板端口 | `13579`（每个实例不同） |
| `CTI_DISABLE_PERMISSION_CHECK` | 禁用权限检查 | `true` 或 `false` |

## 部署步骤

1. 复制配置到实例目录：
```bash
cp config.env.example ~/.agents-to-im-claude/config.env
```

2. 编辑配置：
```bash
$EDITOR ~/.agents-to-im-claude/config.env
```

3. 安装 systemd 服务：
```bash
cp systemd/feishu-claude.service ~/.config/systemd/user/
systemctl --user daemon-reload
systemctl --user enable --now feishu-claude.service
```

4. 验证状态：
```bash
systemctl --user status feishu-claude.service
```
