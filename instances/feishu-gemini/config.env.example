# agents-to-im 实例配置文件（Gemini）

> **注意：** 请勿在版本库中存储真实的密钥！
> 复制此文件到 `~/.agents-to-im-gemini/config.env` 并填写真实密钥。

## 必需配置

```bash
CTI_DEFAULT_WORKDIR=/opt
CTI_FEISHU_APP_ID=cli_XXXXXXXXXXXXX
CTI_FEISHU_APP_SECRET=XXXXXXXXXXXXXXXXXXXXXXXXX
CTI_FEISHU_SHOW_TOOL_CALL_CARDS=false
CTI_DEFAULT_RUNTIME=gemini
CTI_DASHBOARD_PORT=13590
CTI_DISABLE_PERMISSION_CHECK=true

# Gemini 特定配置
CTI_GEMINI_CLI=/opt/gemini/gemini.js
# GEMINI_API_KEY=your-api-key-here

# 分割线配置（可选）
CTI_FEISHU_SHOW_AGENT_DIVIDER=true
CTI_AGENT_NAME=feishu-gemini
CTI_MODEL_PROVIDER=Google
```

## 分割线配置说明

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `CTI_FEISHU_SHOW_AGENT_DIVIDER` | 是否在消息底部显示分割线 | `true` |
| `CTI_AGENT_NAME` | Agent 名称（用于分割线显示） | 实例 profile ID |

## 变量说明

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `CTI_DEFAULT_WORKDIR` | 默认工作目录 | `/opt` |
| `CTI_FEISHU_APP_ID` | 飞书应用 ID | — |
| `CTI_FEISHU_APP_SECRET` | 飞书应用密钥 | — |
| `CTI_FEISHU_SHOW_TOOL_CALL_CARDS` | 是否显示工具调用卡片 | `false` |
| `CTI_DEFAULT_RUNTIME` | 默认运行时 | `gemini` |
| `CTI_DASHBOARD_PORT` | 控制面板端口 | `13590` |
| `CTI_GEMINI_CLI` | Gemini CLI 路径 | `/opt/gemini/gemini.js` |
| `GEMINI_API_KEY` | Gemini API Key | — |

## 部署步骤

1. 复制配置到实例目录：
```bash
mkdir -p ~/.agents-to-im-gemini
cp config.env.example ~/.agents-to-im-gemini/config.env
```

2. 编辑配置：
```bash
$EDITOR ~/.agents-to-im-gemini/config.env
```

3. 验证 gemini CLI 可用：
```bash
node /opt/gemini/gemini.js --version
node /opt/gemini/gemini.js --acp  # 测试 ACP 模式
```

4. 启动实例：
```bash
cd /opt/agents-to-im
CTI_HOME=~/.agents-to-im-gemini node dist/main.js
```
